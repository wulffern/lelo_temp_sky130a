# CLAUDE.md

Everything for this repository lives in [AGENTS.md](AGENTS.md) —
tools, commands, the layout flow, the cicpy placement and routing
APIs, the layout rules, the top-level integration rules and the
spacing rules. **Read that file.**

This file exists only so Claude Code picks the guidance up
automatically. Keep the content in AGENTS.md, which every agent reads;
two copies of the same guidance drift, and this pair already had.
